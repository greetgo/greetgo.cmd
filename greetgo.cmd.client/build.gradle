dependencies {
  compile project(':greetgo.cmd.core')
}

jar {
  manifest {
    attributes 'Class-Path': configurations.compile.collect { it.toString() }.join(' ')
    attributes 'Main-Class': 'kz.greetgo.cmd.client.launcher.Launcher'
  }
}
