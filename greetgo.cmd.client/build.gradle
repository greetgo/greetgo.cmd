dependencies {
  compile project(':greetgo.cmd.core')
}

jar {
  manifest {
    attributes(
      "Class-Path": configurations.compile.collect { it.toString() }.join(' '),
      'Main-Class': 'kz.greetgo.cmd.client.launcher.MainProbe'
    )
  }
}
